SEMCACHE_UPSTREAM_URL=https://api.anthropic.com/v1/messages
SEMCACHE_EMBEDDING_DIM=256
# 0.8 suits the bundled lexical embedder; use ~0.92 with real semantic embeddings.
SEMCACHE_SIMILARITY_THRESHOLD=0.8
SEMCACHE_TTL_SECONDS=3600
